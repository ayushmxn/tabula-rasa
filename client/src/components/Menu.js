import React from "react";
import '../assets/css/Menu.css';

export default class Menu extends React.Component {
    constructor(props)
    {
        super(props);
        this.state = {
            // 
        };
    }

    render()
    {
        return (<nav>
            
        </nav>);
    }
}